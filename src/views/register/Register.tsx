import { motion } from "framer-motion";
import { ReactNode } from "react";
import { Box } from "@chakra-ui/react";
const Register = ()=>{
    return <Box as={motion.div} whileTap={{ scale: 0.8 }} />
}

export default Register;